[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "autonomous-coder"
version = "0.1.0"
description = "A lightweight, terminal-based AI coding agent with pluggable providers."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
autocoder = "agent.cli:main"
